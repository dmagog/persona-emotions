# Full stack (training + vLLM). For inference-only, use requirements-inference.txt instead.
accelerate>=1.7.0
anthropic>=0.37.0
backoff>=2.2.1
bitsandbytes>=0.45.5
datasets>=3.6.0
fire>=0.7.0
openai>=1.70.0
packaging>=25.0
pandas>=2.3.1
peft>=0.15.1
pydantic>=2.11.7
PyYAML>=6.0.2
setuptools>=80.9.0
torch>=2.6.0
tqdm>=4.67.1
transformers>=4.52.3
trl>=0.15.2
vllm>=0.8.5.post1
# unsloth: install separately (see below) — NOT via loose pin on internal PyPI mirrors
