# Figures

This directory contains exploratory plots and report figures produced during the study. The numerical source for each reported result is kept in `runs/`; use `docs/REPRODUCIBILITY.md` to rebuild the two aggregate result tables.

Figure filenames are retained for traceability and should not be read as the numbering of the submitted manuscript.
