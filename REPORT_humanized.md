# Emotion steering vectors in LLMs - промежуточный отчёт

Научная практика, 2-й семестр. Берём метод persona/activation-vectors (контрастные
direction-векторы в пространстве активаций) и переносим его с черт личности на
эмоции. Базовый код - PERSONA (xcfcode), метод - Persona Vectors (arXiv 2507.21509).
Датасет команды - Uliana Kliuchnikova.

## Кратко

Взяли метод контрастных активационных векторов, который работал на чертах личности,
и проверили его на 7 эмоциях ISEAR: можно ли управлять конкретной эмоцией. Можно.
Стиринг наводит целевую эмоцию, и на нормальной выборке (n=56) это значимо для 6/7
эмоций. Сами эмоц-векторы при этом сильно скоррелированы (косинусы 0.45-0.86, и это
переносится на Gemma).

Специфичность мерили аккуратно - судьёй на n=56 с доверительными интервалами. Итог:
стиринг наводит целевую эмоцию значимо по всем 7, target доминирует у 6/7, но рядом
тянется значимая вторичная грусть (sadness +28 [+20,+37]). То есть целевая эмоция
сильнее, а следом подтягивается близкий негативный кластер.

Где метод спотыкается: на простой задаче «навести эмоцию в нейтральном промпте»
обычный промптинг не уступает стирингу и даже чуть специфичнее (7/7 argmax). Так что
практический выигрыш активационного стиринга здесь не виден. Оговорка про измеритель:
энкодер и LLM-судья дают разную картину специфичности, и ни один пока не объявлен
«истиной». Энкодер структурно слеп к guilt/disgust и шумен на малых выборках, но на
остальных 5 эмоциях может быть и менее предвзят. Кто ближе к людям, решаем
человеческой разметкой; основные числа здесь приводим по судье, потому что он
покрывает все 7 эмоций.

Что вышло крепким независимо от всего - методологический результат: для эмоц-направлений
проекция на SAE не расщепляет фичи (Jaccard 0.91), а контраст активаций расщепляет
(0.25). А там, где стиринг реально обходит промптинг - это композиционность. Можно
значимо смешать эмоции (joy+sadness - обе вверх) и вычесть лишнюю (anger-sadness гасит
протечку в грусть, причём системно у всех 5 протекающих эмоций). Инструкцией такое не
дозируешь. Вердикт устойчив: независимый второй судья (qwen-2.5-72b) согласуется сильно
(Pearson 0.90, κ 0.82) и даёт ту же картину. А «минимальность» вектора мы измерили
свипом top-k: большинству эмоций хватает ~4-8 фич.

## Задача и подход

Вопрос простой. Можно ли управлять конкретной эмоцией в генерации LLM, если добавить
в residual stream направление, вытащенное из контрастных пар «эмоциональный vs
нейтральный ответ»? И насколько это управление специфично - целевая эмоция против
остальных?

Пайплайн: датасет (7 эмоций ISEAR), независимое измерение эмоции (классификатор плюс
LLM-судья), извлечение векторов, стиринг, анализ специфичности, первый шаг
расщепления.

## Данные

7 эмоций ISEAR: anger, disgust, fear, guilt, joy, sadness, shame. На каждую примерно
50-60 сценариев, поделённых на train и held-out. По каждой паре инструкций Qwen2.5-3B-
Instruct генерирует два ответа: `pos` (эмоциональный) и `neg` (нейтральный). Это и есть
контрастные пары.

Про train/test split. Векторы, judge-фильтрацию и SAE-фичи считаем на train-сценариях;
стиринг и матрицы специфичности оцениваем на held-out. Отбор направлений и их проверка
не пересекаются по данным.

## 1. Измерение эмоции (мерило)

Два независимых измерителя текста:

- Классификатор-энкодер `text→вектор`: `SamLowe/roberta-base-go_emotions` (GoEmotions,
  28 меток) с агрегацией в 7 ISEAR. Метрика - AUC, разделимость pos vs neg, не зависит
  от порога.
- Pairwise LLM-судья (`meta-llama/llama-3.3-70b-instruct` через OpenRouter): напрямую
  сравнивает pos и neg ответ, шкала 0-100 (50 значит поровну). Это редизайн, на который
  мы согласились с командой, вместо одиночной шкалы.

Энкодер (AUC по эмоциям): disgust 0.82, anger 0.78, shame 0.69, fear 0.70, sadness 0.64,
joy 0.62, guilt 0.62; mean AUC 0.697. Все выше 0.5, то есть pos/neg энкодер разделяет по
всем эмоциям. *(`results/encoder_scoring_qwen2.5-3b.md`)*

Энкодер пришлось доработать. В первой версии нормировка вектора по 7 ISEAR и широкий
joy-кластер залипали (joy: pos 0.948 / neg 0.886, всё стекалось в joy). В v2 нормировку
убрали (GoEmotions всё-таки multi-label) и сузили joy-кластер. Mean shift вырос с 0.127
до 0.173, и появилась устойчивая AUC-метрика.

Судья и кросс-проверка: mean скоры 66.5-77.4, порог 60 проходят 832/920 пар. Оба
измерителя согласны по 6 эмоциям из 7. Расходятся на guilt: судья видит сильный контраст
(74.2), а энкодер слабый (AUC 0.62). Это ограничение энкодера (GoEmotions `remorse` плохо
ловит вину), а не проблема датасета. *(`results/judge_vs_encoder_qwen2.5-3b.md`)*

## 2. Извлечение векторов

Для 832 judge-отфильтрованных пар берём residual-stream активации Qwen2.5-3B, считаем
`mean(pos) − mean(neg)` по слоям и получаем 7 эмоц-векторов `[37, 2048]` (формат как в
PERSONA, совместим со стирингом). Считали на домашнем RTX 2070.

Находка: попарные косинусы между эмоц-векторами 0.58-0.86, все положительные. Векторы не
ортогональны, в них доминирует общая компонента «эмоционально vs нейтрально»; особняком
стоит joy по валентности. *(`results/emotion_vectors_qwen2.5-3b.md`)*

Почему скоррелированы (valence/arousal-чтение). Одна общая ось забирает ~80% дисперсии
7 векторов (на Gemma 76%) - это «эмоциональность vs нейтрально», отсюда высокие косинусы.
В остатке после её вычитания проявляется валентность: остаточная PC1 коррелирует с
канонической валентностью (Spearman +0.70 и на Qwen, и на Gemma), arousal чисто не
выделяется. Эмпирически то же: по баллам судьи joy анти-коррелирует с негативным кластером
(средн. -0.51). То есть запутанность = ось эмоциональности + валентностный остаток.
*(`results/valence_arousal_qwen.md`)*

## 3. Стиринг работает

Добавляем anger-вектор в `model.layers[14]` на нейтральных held-out промптах. Anger-скор
растёт монотонно: 0.033 → 0.226 → 0.465 (coeff 0/4/8). При coeff 8 он даже выше, чем у
инструктированных pos-ответов (0.362). Текст при этом связный и по теме.
*(`results/steering_anger_qwen2.5-3b.md`)*

## 4. Специфичность сырого стиринга

Матрица 7×7 (стиринг к строке → Δ измеряемой эмоции vs baseline, layer 14, coeff 8):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
    anger +0.16 +0.00 -0.00 +0.04 -0.04 +0.23 -0.04
  disgust +0.16 +0.03 +0.13 +0.01 -0.09 +0.17 +0.08
     fear +0.05 +0.01 +0.15 -0.04 +0.00 +0.10 +0.02
    guilt +0.04 +0.00 +0.02 +0.01 +0.04 +0.19 -0.01
      joy -0.00 +0.00 -0.16 -0.03 +0.46 -0.15 +0.07
  sadness +0.08 +0.00 -0.07 -0.04 +0.00 +0.33 -0.02
    shame +0.01 +0.01 +0.09 -0.03 +0.06 +0.18 +0.07
```

- Специфичные (диагональ - максимум): joy (+0.46), sadness (+0.33), fear (+0.15).
- Протекающие: anger (+0.16, но sadness +0.23); disgust, guilt, shame почти не двигают
  свою эмоцию, всё тянет вверх sadness.
- «Аттрактор sadness»: почти любой стиринг поднимает грусть. По этой матрице напрашивался
  вывод, что сырой стиринг даёт общий аффект, а не конкретную эмоцию.
  *(`results/steer_specificity_qwen2.5-3b.md`)*

Тут важная оговорка: это n=14 и энкодер. На Gemma при n=56 «аттрактор sadness» исчезает
(протечка около нуля, см. §8 и `bootstrap_ci_n56.md`). Значит вывод про «общий аффект»
вполне может быть артефактом маленькой выборки, и эту матрицу надо перепроверять на
большем n и судьёй. Что устойчиво при n=56 - сам факт, что стиринг поднимает целевую
эмоцию (диагональ).

## 5. Расщепление (E0): вычитание общей компоненты

Вычли средний по 7 вектор (ренорм к исходной длине), пересчитали матрицу:

- Аттрактор sadness ушёл: протечка в грусть при стиринге к не-sadness эмоциям упала с
  +0.12 до -0.03.
- joy стал чище и сильнее (+0.46 → +0.71); fear остался специфичным.
- Но метод перебарщивает: sadness схлопывается (+0.33 → +0.02). Общий вектор по сути и
  был направлением грусти. Часть эмоций просто сменила протечку (anger→guilt,
  disgust→anger). *(`results/steer_specificity_centered_qwen2.5-3b.md`)*

Mean-subtraction - грубый инструмент. Он убирает доминирующую ось, но не изолирует каждую
эмоцию. Отсюда мотивация перейти к SAE: разреженный разбор, «минимальный эмоц-вектор».

## 6. Расщепление через SAE (E1)

Перенесли векторы на gemma-2-2b (под неё есть готовые SAE GemmaScope). Эмоц-векторы Gemma
скоррелированы так же (косинусы 0.45-0.83), то есть запутанность не привязана к
конкретной модели. Дальше спроецировали эмоц-направления на энкодер GemmaScope SAE
(`layer_12/width_16k`) и взяли топ-фичи.

- Наивная проекция: топ-фичи почти одинаковые у всех эмоций, mean Jaccard 0.91.
- После центрирования (вычесть общую компоненту, потом проекция): 0.84, почти не помогло.

Вывод: проекция difference-направления на SAE не изолирует эмоция-специфичные фичи.
Несколько доминирующих фич забирают проекцию у всех. Запутанность держится на всех срезах:
косинусы, специфичность, центрирование, SAE-проекция. *(`results/gemma_sae_layer12.md`)*

## 7. Контрастные активации SAE дают эмоция-специфичные фичи (E1c)

Правильный метод другой: прогнать реальные pos/neg ответы через Gemma+SAE и ранжировать
фичи по `mean(pos)−mean(neg)` активаций (контраст гасит общий фон). И тут эмоции занимают
разные фичи:

| метод | mean off-diag Jaccard |
|-------|----------------------:|
| проекция направления, наивная | 0.91 |
| проекция, центрированная | 0.84 |
| контрастные активации | 0.25 (min 0.08) |

Общие фичи есть (общий аффект), но у каждой эмоции свои (anger 8517/16003/14599,
guilt 1041/8567, fear 621, joy 6648). Методологический урок: узким местом был не SAE, а
способ. Проекция направления проваливается, контраст активаций расщепляет. Получаем
разреженный эмоция-специфичный набор фич. *(`results/sae_contrastive_layer12.md`)*

### 7а. Сколько фич достаточно - свип top-k (W6, «минимальность» измерена)

Собирали steering-вектор каждой эмоции только из top-k контрастных фич (k∈{1,2,4,8,20})
и судили эффект (судья плюс bootstrap CI, n=28 held-out):

| k фич/эмоцию | средний эффект диагонали | значимых диагоналей |
|---:|---:|:--:|
| 1 | +5 | 1/7 |
| 4 | +19 | 5/7 |
| 8 | +27 | 5/7 |
| 20 | +40 | 7/7 |

- Качество монотонно растёт с k. Горстки ~4-8 фич хватает, чтобы большинство эмоций
  стирилось значимо; все 7 выходят на значимость только на полных 20.
- Сколько фич нужно, зависит от эмоции. guilt стирится одной фичей (+42 при k=1), а joy
  не заводится до k=20 (он размазан по многим фичам).
- Меньше фич не значит чище. Отношение сигнал/протечка при урезании не улучшается (k=4
  это примерно 1:1, k=20 это 1.5:1). Топ-фичи несут и общий аффект, поэтому «минимальный»
  вектор просто слабее, а не специфичнее. Это уточняет прежнее вольное «минимальный».
  *(`results/topk_sweep_gemma.md`)*

### 7б. Почему расщепление не переходит в специфичность (механизм)

Фичи расщепляются на уровне индексов (Jaccard 0.25), но steering-вектор - это они,
декодированные обратно в residual. Сравнили попарную запутанность (средний off-diag
косинус, layer 12) трёх наборов:

| набор векторов | mean off-diag cos | остаток общей оси |
|---|---:|---:|
| сырые | +0.66 | 0.84 |
| центрированные (raw − общая ось) | −0.16 | 0.27 |
| SAE-фичи (декод) | +0.61 | 0.48 |

Декодированный SAE-вектор почти так же запутан, как сырой (+0.61 vs +0.66), и держит
~половину общей «эмоциональной» оси (0.48). Расщепление на уровне фич плохо переживает
декод (строки W_dec сами скоррелированы) - вот почему специфичность стиринга не растёт
(W2). Центрирование даёт меньший косинус (−0.16), но ценой пере-коррекции (§5 схлопывает
sadness). Вывод: разреженность на уровне фич не равна ортогональности направлений стиринга.
*(`results/mechanism_ablation_gemma.md`)*

## 8. Стиринг через SAE-фичи

Декодировали отобранные эмоц-фичи обратно в residual (Σ Δ·W_dec[f]) и стирили ими
gemma-2-2b, рядом с сырым вектором (матрица специфичности, layer 12, coeff 8):

| | mean диагональ | target=argmax | протечка в sadness |
|---|---:|---:|---:|
| сырой вектор | +0.20 | 3/7 | +0.10 |
| SAE-фичи | +0.16 | 3/7 | +0.05 |

Полные матрицы Δ (стиринг к строке → измеряемая эмоция), сырой vs SAE-фичи:

```
сырой       ange  disg  fear  guil   joy  sadn  sham
   anger +0.23 +0.01 -0.01 -0.01 -0.17 +0.25 +0.11
 disgust +0.22 +0.15 -0.03 +0.08 -0.18 +0.19 -0.05
    fear +0.09 +0.01 +0.22 -0.01 -0.13 +0.03 +0.04
   guilt +0.02 +0.00 +0.10 +0.06 -0.18 +0.22 -0.05
     joy -0.03 -0.01 -0.07 -0.02 +0.39 -0.16 +0.01
 sadness +0.11 +0.02 -0.08 -0.03 -0.08 +0.22 -0.06
   shame +0.21 +0.06 +0.03 +0.09 -0.13 +0.06 +0.10

SAE-фичи    ange  disg  fear  guil   joy  sadn  sham
   anger +0.14 +0.04 +0.19 +0.03 -0.16 +0.01 +0.06
 disgust +0.02 +0.06 +0.11 +0.00 -0.05 +0.04 +0.07
    fear +0.02 +0.00 +0.13 -0.02 -0.16 +0.06 -0.00
   guilt +0.12 +0.01 -0.11 +0.06 -0.18 +0.22 -0.04
     joy -0.03 -0.01 -0.06 +0.03 +0.40 -0.04 -0.00
 sadness +0.06 +0.02 +0.09 -0.02 -0.16 +0.19 -0.06
   shame +0.19 +0.03 +0.03 -0.01 -0.16 +0.01 +0.16
```

На маленькой выборке (n=14, энкодер) казалось, что feature-стиринг вдвое срезает «аттрактор
sadness» (raw +0.10 → SAE +0.05). При n=56 это не подтвердилось: энкодерная протечка около
нуля у обоих (raw +0.011 [−0.08,+0.09], SAE +0.020 [−0.07,+0.10]), разницы между raw и
SAE-фичами нет. Правда, энкодер тут и сам ненадёжен, поэтому достоверный вердикт по
специфичности даёт судья на n=56 (ниже). Что устойчиво по энкодеру при n=56: диагонали
значимы у 6/7, стиринг наводит целевую эмоцию. *(`results/steer_sae_feature_gemma.md`,
`results/bootstrap_ci_n56.md`)*

Связность. Стиринг при coeff 8 текст не ломает: судья по связности даёт всем условиям ≥
82/100 (baseline 95), падение умеренное (−3 у sadness/guilt и до −13 у disgust). То есть
специфичность мы мерили на связных генерациях, а не на «каше». *(`results/coherence_gemma_saefeat.md`)*

Проверка судьёй (guilt/disgust - слепая зона энкодера). Просудили стиреные ответы
LLM-судьёй: целевая эмоция наводится по всем проверенным. guilt 32→78 (+46), disgust
10→37, anger 14→41, joy 21→55. «Провал» guilt/disgust в энкодерной матрице был артефактом
измерения (GoEmotions их не видит), а не проблемой стиринга. *(`results/judge_steered_gemma_saefeat.md`)*

Достоверная матрица специфичности (судья, n=56, с CI). Просудили SAE-feature стиреные
ответы по всем 7 эмоциям на 56 промптах на условие (диагональ в `[]`):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
   anger   [+36]  +11   +28   +20   -12   +32   +24
 disgust    +28  [+29]  +39   +22    -7   +34   +29
    fear    +16   +11  [+50]  +19    -5   +24   +16
   guilt     +9    +9   +20  [+49]  -18   +44   +42
     joy     -8    -6   -14   -12  [+33]  -12   -13
 sadness    +24   +17   +38   +32   -10  [+44]  +26
   shame    +28   +16   +48   +54   -18   +48  [+56]
```

Все 7 диагоналей значимы (bootstrap 95% CI без 0): shame +57 [46,67], fear +48 [36,60],
guilt +49 [37,60], sadness +44 [33,54], anger +36 [25,47], joy +34 [20,48], disgust +29
[19,40]. target=argmax у 6/7 (кроме disgust: fear +39 > disgust +29). Протечка в sadness
+28 [+20,+37] значима: рядом с целевой эмоцией реально подтягивается негативный кластер
(sadness/guilt/fear/shame растут вместе), но целевая почти всегда сильнее.
*(`results/judge_specificity_n56.md`)*

Оговорка про измеритель. Энкодер и судья дают разную картину специфичности: на n=14
энкодер давал ложную структуру, на n=56 наоборот смыл протечку (~0), а судья её видит
(+28). Это не значит автоматически, что энкодер «хуже»: возможен и обоснованный сдвиг
(судья тоже невалидирован и может иметь свою предвзятость). Твёрдое ограничение
энкодера - он структурно слеп к guilt/disgust (в GoEmotions их нет). Поэтому основные
числа приводим по судье (он покрывает все 7 эмоций), а вопрос «кто ближе к людям»
оставляем открытым и решаем человеческой разметкой (~300 примеров, single-emotion
0-100; план 5.3).

Baseline «промптинг» (W7): стиринг его не превосходит. Сгенерили с pos-инструкцией эмоции
(без вектора) на тех же промптах и просудили (n=56, CI). Промптинг сильнее по диагонали у
5/7 (joy +57 vs +34, guilt +58 vs +49, anger и disgust выше), специфичнее (протечка
sadness +18 [9,27] против +28 [20,37] у стиринга) и даёт target=argmax 7/7 (против 6/7).
То есть на задаче «навести эмоцию в нейтральном промпте» активационный стиринг через
SAE-фичи не выигрывает у инструкции, даже чуть уступает. Отличительную ценность стиринга
(композиционность, управление без промпта) здесь не проверяли.
*(`results/steer_vs_prompt_n56.md`)*

## 9. Композиционность - где стиринг обходит промптинг

Стирим линейными комбинациями SAE-feature векторов (судья n=56, CI). Это как раз то, что
промптингом не дозируешь:

- Сплав `joy+sadness`: одновременно joy +17 [+3,+31] и sadness +26 [+14,+38], обе значимо
  вверх. Получается управляемый «горько-сладкий» микс (у joy-одного sadness −13, у
  sadness-одного joy −11).
- Вычитание `anger − sadness` убирает «аттрактор sadness»: ко-активация грусти падает с
  +31 до −6 [−16,+5] (снижение +37 [+25,+49], значимо), а anger остаётся значимым и
  доминирующим (+37 → +20). Арифметика векторов хирургически удаляет лишнюю эмоцию.
- `joy − sadness` заостряет цель: joy +33 → +50 [+38,+62], sadness глубже, −14 → −28
  [−36,−20].

Системность вычитания (X − sadness для всех протекающих эмоций, судья n=56 плюс CI).
Проверили, что `anger−sadness` это не единичный трюк: прогнали вычитание грусти для всех
5 эмоций с заметной протечкой. Снижение sadness значимо у всех 5 (CI без 0): anger +39
[+27,+51], disgust +29 [+17,+41], fear +33 [+21,+45], guilt +41 [+31,+50], shame +34
[+25,+44]. Целевая эмоция при этом сохраняется значимо у 4/5 (anger +18, disgust +11,
fear +20, shame +34). Исключение - guilt: вычитание гасит и саму вину (+3 [−9,+15], не
значимо). guilt и sadness в этой модели слиты в один кластер (это видно и по судейской
матрице guilt~shame~sadness). *(`results/compose_subtract_gemma.md`)*

Вот это и есть отличительная ценность активационного стиринга: дозируемое смешивание и
вычитание эмоций, включая системное гашение найденного аттрактора. Промптингом так не
сделаешь. Цена вычитания - небольшое ослабление целевой эмоции, регулируется
коэффициентом; для очень близких эмоций (guilt↔sadness) вычитание не разделяет их чисто.
*(`results/compose_gemma.md`, `results/compose_subtract_gemma.md`)*

## Выводы

1. Стиринг наводит целевую эмоцию, и это устойчиво. При n=56 диагональ значима у 6/7
   эмоций, на связном тексте (≥82/100). Твёрдый положительный результат.
2. Специфичность (достоверно, судья n=56 плюс CI): стиринг наводит целевую эмоцию значимо
   по всем 7, target доминирует у 6/7; рядом идёт значимая вторичная ко-активация грусти
   (+28 [+20,+37]) и близкого негативного кластера. Управление в основном специфично, с
   честной вторичной протечкой в смежные эмоции.
3. Векторы эмоций сильно скоррелированы (косинусы 0.45-0.86, и на Qwen, и на Gemma).
   Простое центрирование убирает доминирующую ось, но перебарщивает. Грубый инструмент.
4. Крепкий методологический результат: для эмоц-направлений проекция на SAE не расщепляет
   (Jaccard 0.91), а контраст активаций расщепляет (0.25). Узкое место - метод, а не SAE.
   Получаем разреженный эмоция-специфичный набор фич. Минимальность измерили свипом top-k
   (§7а): ~4-8 фич хватает большинству эмоций (guilt - 1, joy - все 20), но урезание фич
   специфичность не повышает. Важная граница (W2): расщепление реально на уровне отбора
   фич (Jaccard 0.25), но в поведенческую специфичность стиринга при равной силе не
   переходит - при уравненном эффекте диагонали SAE-вектор не специфичнее сырого.
5. Урок про измеритель: энкодер и судья дают разную картину специфичности (на n=14 энкодер
   давал ложную структуру, на n=56 смыл протечку, которую судья видит). Прежний claim
   «SAE-фичи срезают протечку» оказался артефактом энкодера на малом n. Кто из измерителей
   точнее - пока открытый вопрос: энкодер слеп к guilt/disgust и шумен, но на остальных 5
   может быть менее предвзят, чем (тоже невалидированный) судья. Основные числа приводим по
   судье (покрывает все 7), а арбитраж «энкодер vs судья» делаем человеческой разметкой
   (план 5.3) с CI.
6. Граница (промптинг-baseline): на задаче «навести эмоцию в нейтральном промпте» простой
   промптинг не уступает стирингу и даже специфичнее (7/7 argmax). Практического выигрыша
   активационного стиринга здесь нет.
7. Где стиринг всё-таки ценен - композиционность (значимо, судья плюс CI): дозируемый сплав
   (joy+sadness, обе вверх) и вычитание (anger−sadness гасит «аттрактор sadness», anger
   остаётся доминирующим; joy−sadness заостряет joy). Вычитание грусти системно убирает
   аттрактор у всех 5 протекающих эмоций (снижение значимо везде, целевая эмоция сохраняется
   у 4/5; граница - слитые guilt↔sadness). Векторная арифметика даёт контроль, недостижимый
   промптингом. Вот это и есть отличительная ценность метода.
8. Вердикт устойчив к выбору судьи (W4): независимый судья другого семейства (qwen-2.5-72b)
   на тех же ответах согласуется с основным сильно (Pearson 0.90, Cohen's κ 0.82, argmax
   6/7) и даёт ту же картину (все 7 диагоналей значимы, значимая протечка sadness).
   Единственное расхождение - guilt↔sadness.

## Угрозы валидности

Честно перечисляем, где результаты пока слабы (план усиления - `IMPROVEMENT_PLAN.md`):

- Выборка/погрешности (W1), частично закрыто. Главная (судейская) матрица специфичности
  теперь на n=56 с bootstrap 95% CI (все 7 диагоналей значимы, протечка sadness значима).
  Coeff-matched raw vs SAE тоже на CI (W2). Остаётся малым только ранняя матрица §4 на
  Qwen (n=14), её ещё можно добить до n=56.
- Магнитуда raw vs SAE-фичи (W2), закрыто, результат отрезвляющий. Свели coeff {4,8,16}
  для обоих типов и построили фронтир «протечка vs эффект диагонали» (судья плюс CI). При
  одном coeff SAE стирит слабее (диагональ 16<20, 39<45, 60<70). При сопоставимом эффекте
  диагонали SAE не специфичнее raw, а на высокой силе даже протекает больше (при диаг ~60
  протечка raw ≈27 vs sae +36). Видимость «SAE меньше протекает» оказалась артефактом
  магнитуды: слабый стиринг тривиально меньше протекает. Расщепление SAE реально на уровне
  отбора фич (Jaccard 0.25), но в поведенческую специфичность при равной силе не переходит
  (`results/coeffmatch_gemma.md`).
- Смешение модель×измеритель (W3), закрыто по основной линии. Вся достоверная линия
  результатов идёт на одной модели одним мерителем: gemma-2-2b плюс LLM-судья - стиринг
  (§8), специфичность n=56 (§8), композиция (§9), системное вычитание (§9), свип top-k
  (§7а), валидация вторым судьёй (W4). Ранний Qwen плюс энкодер (§4-5) - разведочная часть,
  и именно её несостыковки (энкодер смыл протечку) мы и показываем как урок про измеритель.
  Сквозной single-model/single-measurer прогон есть; смешение осталось только в ранних
  разделах, помеченных как exploratory.
- Судья один и невалидирован (W4), закрыто по части второго судьи. Пересудили те же n=56
  ответов независимым судьёй другого семейства (qwen-2.5-72b): согласие сильное - Pearson
  r=0.90, Spearman ρ=0.90 (2940 пар), Cohen's κ=0.82, argmax по диагонали совпал 6/7. Оба
  судьи независимо дают все 7 диагоналей значимыми и значимую вторичную протечку sadness
  (llama +28 [+20,+37], qwen +24 [+16,+31]). Вердикт не зависит от одной модели. Единственное
  расхождение - guilt (qwen видит там sadness), та же слитость guilt↔sadness. Остаётся
  опционально добавить человеческие метки (`results/judge_agreement_n56.md`).
- Связность (W5), измерена (§8): при coeff 8 текст не ломается (≥82/100), но оценка тем же
  семейством судьи и на n=14.
- «Минимальность» (W6), измерена. Свип top-k (§7а): качество стиринга растёт с k, ~4-8 фич
  хватает большинству эмоций (значимо), guilt - 1 фича, joy - все 20; урезание фич
  специфичность не повышает (сигнал/протечка не растёт). Слово «минимальный» теперь
  подкреплено числами.
- Baseline «промптинг» (W7), закрыто, и результат отрезвляющий: промптинг не уступает
  стирингу (даже специфичнее). Выигрыш стиринга над инструкцией на этой задаче не показан (§8).
- «Аттрактор sadness» (W8), закрыто. Судья на n=56 с CI: ко-активация sadness значима
  (+28 [+20,+37]), но вторична относительно целевой эмоции. Реальный эффект, не артефакт,
  подтверждён вторым судьёй (qwen: +24 [+16,+31], см. W4). Нюанс: в нейтральных
  (нестиреных) ответах sadness и так самая высокая эмоция (~32/100, оба судьи). Частично
  это базовый меланхоличный приор промптов, но протечка считается как Δ над baseline, то
  есть стиринг поднимает грусть поверх приора (`results/baseline_emotion_prior.md`).
- Прочее: эмоции инструкция-индуцированы (не естественны); стиринг `positions=all`, один
  слой, без подбора на эмоцию; guilt - всего 42 пары.

## Дальше

Сделано в ходе усиления (см. `IMPROVEMENT_PLAN.md`): bootstrap CI, судья n=56, coherence,
промптинг-baseline (W7), композиционность и системное вычитание (§9), свип top-k /
«минимальность» (§7а, W6), второй судья (W4), базовый приор (W8), сквозная линия на одной
модели/мерителе (W3), coeff-matched raw vs SAE (W2). Осталось:

- Больше моделей и слоёв (перенос и обобщаемость); SAE на нескольких слоях.
- Естественные (не инструкция-индуцированные) эмоции - внешняя валидация векторов.
- Опционально: 20-30 человеческих меток к судье; механизм «почему SAE помогает» (top-k
  фичи vs «raw минус общие фичи»).
- Согласовать рамку новизны и деление вклада с Уляной.

## Воспроизводимость

Репозиторий: github.com/dmagog/persona-emotions, ветка `emotion-encoder`. Код в `emotion/`:
измерение - `space`, `encoder`, `classifier_encoder`, `metrics`, `score_csv`; судья -
`pairwise_judge`, `run_pairwise_judge`, `judge_steered`, `judge_specificity`,
`judge_agreement` (согласие судей); статистика - `bootstrap_ci`, `coherence_check`; векторы
и стиринг - `extract_vectors`, `steer_eval`, `steer_specificity`, `steer_compose`
(сплав/вычитание); SAE - `gemma_sae`, `sae_contrastive`, `build_sae_vectors`; свипы -
`steer_topk_sweep`+`judge_topk_sweep`+`topk_summary` (минимальность),
`steer_coeffmatch_sweep`+`judge_coeffmatch_sweep`+`coeffmatch_summary` (raw vs SAE при равной
магнитуде). Воспроизводящие ноутбуки - `notebooks/`. Артефакты с числами и матрицами -
`results/*.md` плюс CSV/JSON. Компьют: домашний RTX 2070 (извлечение/стиринг/SAE/свипы на
Qwen2.5-3B и gemma-2-2b), Kaggle (энкодер на CPU), OpenRouter (LLM-судьи llama-3.3-70b и
qwen-2.5-72b).

## Вклад

- Uliana Kliuchnikova: датасет (7 эмоций), генерация ответов, базовый пайплайн.
- Данная работа: блок измерения (энкодер плюс AUC, pairwise-судья плюс кросс-проверка),
  извлечение эмоц-векторов и стиринг, матрицы специфичности, расщепление (центрирование E0;
  SAE-проекция E1; контрастные активации SAE E1c - минимальный эмоц-вектор), стиринг через
  SAE-фичи и судейская оценка специфичности; усиление методологии: bootstrap CI, судья на
  n=56, проверка связности, baseline-промптинг, композиционность и системное вычитание, свип
  top-k («минимальность»), валидация вторым судьёй (согласие), базовый эмоц-приор,
  coeff-matched сравнение raw vs SAE.
