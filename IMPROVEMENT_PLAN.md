# План усиления исследования

Цель: превратить «аккуратные точечные числа» в результат **с погрешностями и
честными контролями**, и вести изложение с самого крепкого (методологического)
вклада. Отмечаем прогресс галочками.

Связь с критикой (слабые места, по убыванию опасности):
W1 — нет статмощности (n=14, нет CI); W2 — raw vs SAE-фичи не контролирует
магнитуду; W3 — смешение модель×измеритель; W4 — судья один, невалидирован, он же
фильтр; W5 — связность текста не измерена; W6 — «минимальный» не показан; W7 —
нет baseline «промптинг»; W8 — «аттрактор sadness» недо-проверен.

## Фаза 1 — текст и рамка (без прогонов, сразу) ✅ ГОТОВО

- [x] 1.1 Раздел «Угрозы валидности» в REPORT.md (честный список W1–W8)
- [x] 1.2 Явно прописать train/test split (векторы/фичи — из train-ответов, оценка — на held-out промптах)
- [x] 1.3 Снизить триумфальность («РАБОТАЕТ», «ключевой результат», «цикл замкнут») под силу статистики
- [x] 1.4 Вынести методологический результат (проекция vs контраст, 0.91→0.25) как главный вклад

## Фаза 2 — статистика и контроли (GPU/API, дёшево, высокий impact)

- [x] 2.1a **bootstrap CI** на имеющихся данных (n=14): показал, что claim «срезает протечку» НЕ значим, значимы только joy/anger (`emotion/bootstrap_ci.py`, `results/bootstrap_ci_gemma.md`) → W1
- [x] 2.1b-энкодер n=56 (×4) + CI: диагонали значимы 6/7 (стиринг работает), НО «аттрактор sadness» и «срезание протечки» — артефакты n=14 (при n=56 протечка ~0). REPORT/Выводы поправлены (`results/bootstrap_ci_n56.md`) → W1, W8
- [x] 2.1b-судья: судейская матрица на n=56 + bootstrap CI — ДОСТОВЕРНЫЙ вердикт: все 7 диагоналей значимы (стиринг работает, target доминирует 6/7), протечка sadness значима но вторична (+28 [+20,+37]); энкодер для специфичности ненадёжен (`results/judge_specificity_n56.md`) → W1, W8 закрыты
- [x] 2.2 **Coherence-judge** на стиреные выходы: текст не ломается (≥82/100, baseline 95), disgust теряет больше всех (`emotion/coherence_check.py`, `results/coherence_gemma_saefeat.md`) → W5
- [x] 2.3 **Coeff-matched** raw vs SAE-фичи (судья + CI): свип coeff {4,8,16}, фронтир «протечка vs эффект диагонали». При одном coeff SAE стирит слабее (диаг 16<20, 39<45, 60<70); при сопоставимом эффекте SAE НЕ специфичнее raw (на высокой силе даже протекает больше). «SAE меньше протекает» было артефактом магнитуды. Расщепление реально на уровне отбора фич, в поведенческую специфичность при равной силе не переходит (`results/coeffmatch_gemma.md`) → W2 закрыт
- [x] 2.4 Baseline **«промптинг»** (судья n=56 + CI): промптинг не уступает стирингу — сильнее на 5/7, специфичнее (sadness leak +18 vs +28), argmax 7/7 vs 6/7. Выигрыш стиринга над инструкцией не показан (`results/steer_vs_prompt_n56.md`) → W7 закрыто

## Фаза 3 — целостность и глубина (средне)

- [x] 3.1 Сквозной end-to-end на одной модели одним мерителем (gemma-2-2b + судья): стиринг, специфичность n=56, композиция, вычитание, top-k, второй судья — всё на одной связке. Qwen+энкодер (§4–5) помечены как ранняя разведка. W3 закрыт по основной линии (правка в REPORT «Угрозы валидности»)
- [x] 3.2 Свип **top-k** фич (судья + CI, n=28): качество монотонно растёт с k (k=1 +5, k=4 +19, k=8 +27, k=20 +40; значимых диагоналей 1→5→5→7). ~4–8 фич хватает большинству, guilt — 1 фича, joy — все 20; урезание НЕ повышает специфичность (сигнал/протечка не растёт). «Минимальность» измерена, не заявлена (`results/topk_sweep_gemma.md`) → W6
- [~] 3.3 Второй судья (qwen-2.5-72b) на тех же n=56 ответах: согласие сильное — Pearson 0.90, Spearman 0.90, Cohen's κ 0.82, argmax 6/7; оба судьи дают все 7 диагоналей значимыми + значимую протечку sadness (llama +28, qwen +24). Вердикт НЕ артефакт одной модели; единств. расхождение — guilt↔sadness. Остаётся опционально: 20–30 человеческих меток (`results/judge_agreement_n56.md`) → W4 (второй судья закрыт)
- [ ] 3.4 Абляция «почему SAE помогает»: top-k фичи vs «raw минус общие фичи» → механизм
- [~] 3.5 Контроль «аттрактора sadness»: базовый уровень — sadness самая высокая эмоция и БЕЗ стиринга (~32/100, оба судьи) → «аттрактор» частично базовый приор, но протечка считается над baseline (эффект реален сверх приора). Остаётся (GPU): переупорядочивание эмоций (`results/baseline_emotion_prior.md`) → W8

## Фаза 4 — новизна (для научности, согласовать с Уляной)

- [x] 4.1 Композиционность (значимо, судья+CI): сплав joy+sadness (обе вверх +17/+26), вычитание anger−sadness гасит аттрактор (sadness +31→−6, anger остаётся +20), joy−sadness заостряет joy (+33→+50). ЭТО ценность стиринга над промптингом (`results/compose_gemma.md`)
- [x] 4.1b Системность вычитания (судья n=56 + CI): X−sadness для всех 5 протекающих эмоций. Снижение sadness значимо у ВСЕХ 5 (anger +39, disgust +29, fear +33, guilt +41, shame +34); целевая эмоция сохраняется значимо у 4/5 (anger/disgust/fear/shame), guilt — исключение (+3, слит с sadness). Вычитание — системный инструмент, не единичный трюк (`results/compose_subtract_gemma.md`)
- [ ] 4.2 Больше моделей и слоёв; SAE на нескольких слоях
- [ ] 4.3 Естественные эмоции (не инструкция-индуцированные) для валидации векторов

## Фаза 5 — после синка с Ульяной (28.05): расширение + разделение труда

Главная общая идея, как сформулировала Ульяна: **минимальное представление эмоций**
(наш top-k/SAE-результат) + контроль векторами понятнее, чем промптом. Разделение:
Ульяна берёт независимый трек, чтобы не пересекаться; судейский промпт общий — наш.

Разделение труда:
- **Ульяна (её трек):** «традиционные методы» (реализованы, надо проверить); запуск
  расширения по моделям/слоям; добавляет 3-го судью (deepseek), т.к. наших двух у неё
  нет. Использует НАШ pairwise-промпт судьи (для финального сравнения).
- **Мы:** SAE/стиринг/композиция/судья-пайплайн (готово); A+V-сравнение; вклад в
  разметку и в выбор измерителя; сборка текста.

- [ ] 5.1 **A+V (valence-arousal) сравнение** — соотнести нашу корреляционную структуру
      эмоций (доминирующая ось ≈ sadness/негатив) с моделью valence-arousal и/или с
      корреляцией из статьи-источника энкодера (GoEmotions). Цель: объяснить, ПОЧЕМУ
      сырые эмоции скоррелированы (это валентностная ось), и дать интерпретацию. Без GPU
      (корреляции на готовых скорах / лексикон VAD). Связь: §2, §4, Вывод 3.
- [ ] 5.2 **Третий судья (deepseek)** — расширить inter-judge agreement (W4) с 2 до 3
      судей. Запускает Ульяна. Усиливает «вердикт не артефакт одной модели».
- [~] 5.3 **Человеческая разметка (~300 items)** — РЕШИТЬ, кто ближе к людям:
      энкодер или LLM-судья. Закрывает W4 по-настоящему И разрешает спор «энкодер
      хуже или просто менее предвзят». Протокол зафиксирован: **single-emotion 0–100**.
      **Харнесс готов** (`emotion/build_label_sheet.py` → `results/human_label_sheet.csv`
      300 items, баллы моделей скрыты в `human_label_key.csv`; разметку считает
      `emotion/label_correlation.py`: human↔encoder vs human↔judge, Spearman/Pearson,
      overall + по эмоциям). Осталось: проставить human_0_100 (мы + Ульяна) и прогнать.
- [ ] 5.4 **Стиринг на рандомных/нетематических промптах** — проверить, работает ли
      управление вне eval-промптов (внешняя валидность, «контроль без промпта»). Связь
      с 4.3. Можно у Ульяны.
- [ ] 5.5 **Свип по слоям** (история первого полугодия) — на каких слоях стиринг/
      расщепление сильнее. Часть 4.2. Можно у Ульяны.
- [ ] 5.6 **Расширение по моделям** (4.2): qwen-1.7b/20b — только если есть SAE;
      gemma-1/3; llama-варианты. Сырой стиринг переносится легко; SAE-часть ограничена
      наличием готовых SAE. Запускает Ульяна.

Методологическая развилка (поднял Ульяна, важно для фрейминга):
- Ульяна резонно замечает: «у энкодера меньше разница» не обязательно значит «энкодер
  хуже» — возможно, это обоснованный сдвиг (LLM-ки «так видят»), и тогда основные
  результаты надёжнее показывать **энкодером**. Контр-довод: энкодер структурно слеп к
  guilt/disgust (в GoEmotions их нет) и при n=56 смыл реальный эффект. Истину рассудит
  **5.3 (человеческая разметка)**: до неё — не объявлять победителя, в REPORT смягчить
  «энкодер ненадёжен» до «энкодер слеп на guilt/disgust и шумен; для остальных 5 —
  возможно, менее предвзят; решит человеческая корреляция». Действие: подправить
  формулировку в REPORT/выводах после 5.3 (или мягко сейчас).

## Журнал

- (старт) план создан; берёмся за Фазу 1.
- Фаза 1 готова: в REPORT.md добавлен раздел «Угрозы валидности» (W1–W8), явный
  train/test split, снижена триумфальность в заголовках/выводах, методологический
  вклад вынесен в «Кратко» как главный.
- Фаза 2 (частично): 2.1a CI на n=14, 2.2 coherence — ок. **2.1b — ключевой
  поворот:** на n=56 «аттрактор sadness» и «SAE срезает протечку» оказались
  артефактами малой выборки (протечка ~0), зато «стиринг наводит целевую эмоцию»
  стало значимым (6/7). REPORT и Выводы честно поправлены. Осталось: судья на n=56
  (2.1b-судья), coeff-matched (2.3), промптинг-baseline (2.4).
- Фаза 2 (продолжение):
  - **2.1b-судья (достоверный вердикт).** Судья на n=56 + CI: все 7 диагоналей
    значимы (стиринг работает, target доминирует 6/7), а протечка sadness **значима**
    (+28 [+20,+37]). То есть энкодер на n=56 её ОШИБОЧНО смыл — энкодер для
    специфичности ненадёжен; верить судье. «Аттрактор» реален, но вторичен. (W1/W8).
  - **2.4 промптинг-baseline (отрезвляюще).** Судья n=56: промптинг не уступает
    стирингу — сильнее на 5/7, специфичнее (sadness leak +18 vs +28), argmax 7/7 vs
    6/7. Выигрыш активационного стиринга над инструкцией не показан. (W7).
  - Закрыты W1(частично)/W5/W7/W8. Осталось 2.3 (coeff-matched), Фаза 3/4, синк с Уляной.
- Фаза 3 (целостность):
  - **3.3 второй судья (W4).** Пересудили те же n=56 SAE-feature ответов независимым
    судьёй qwen-2.5-72b (другое семейство). Согласие сильное: Pearson 0.90,
    Spearman 0.90 (2940 пар), Cohen's κ 0.82, argmax по диагонали 6/7. Оба судьи
    независимо: все 7 диагоналей значимы + значимая вторичная протечка sadness
    (llama +28 [+20,+37], qwen +24 [+16,+31]). Вердикт специфичности не артефакт
    одной модели. Единственное расхождение — guilt (qwen→sadness), та же слитость.
    Починен баг устойчивости судьи; добавлен инкрементальный JSONL-кэш с резюмом.
    REPORT (W4/W8) и план обновлены. Остаётся опционально: человеческие метки.
  - **3.2 top-k свип (W6).** Прогон на RTX 2070 (задача EmoTopk): для k∈{1,2,4,8,20}
    собирали вектор из top-k контрастных фич, стирили, судили (llama + bootstrap CI).
    Кривая: эффект монотонно растёт с k (+5→+40), значимых диагоналей 1→5→5→7. ~4–8
    фич достаточно большинству эмоций; guilt стирится 1 фичей, joy требует все 20;
    урезание фич НЕ повышает специфичность. «Минимальность» измерена. Инфра-урок:
    судья падал на 6340/7056, но инкрементальный JSONL-кэш дал резюм с места обрыва
    (теперь окупился). REPORT §7а + W6 обновлены.
- Фаза 4 (новизна):
  - **4.1 Композиционность** (судья n=56 + CI): сплав joy+sadness (обе значимо вверх),
    вычитание anger−sadness гасит «аттрактор sadness» (anger остаётся доминирующим),
    joy−sadness заостряет joy. Это отличительная ценность стиринга над промптингом.
  - **4.1b Системность вычитания** (судья n=56 + CI): X−sadness для всех 5 протекающих
    эмоций. Снижение sadness значимо у ВСЕХ 5; целевая эмоция сохраняется значимо у 4/5.
    Исключение — guilt (слит с sadness в один кластер). Вывод: векторная арифметика —
    системный, статистически значимый инструмент подавления off-target эмоции; граница —
    очень близкие эмоции. REPORT §9 и Выводы(7) обновлены.
- Фаза 3 (продолжение) + Фаза 2 (добивка):
  - **3.2 top-k свип (W6).** Качество монотонно растёт с k (+5→+40), значимых
    диагоналей 1→5→5→7; ~4–8 фич большинству, guilt 1, joy 20; меньше фич ≠ чище.
  - **3.3 второй судья (W4).** qwen-2.5-72b на тех же n=56: Pearson 0.90, κ 0.82,
    argmax 6/7; вердикт не артефакт одной модели. Инфра: укреплён судья + резюм-кэш.
  - **3.1/W3.** Сквозная линия на gemma+судья; Qwen+энкодер помечены как разведка.
  - **3.5/W8 (частично).** Базовый приор: sadness и без стиринга самая высокая (~32).
  - **2.3/W2.** Coeff-matched raw vs SAE: при равном эффекте диагонали SAE НЕ
    специфичнее raw — прежнее «меньше протечки» было артефактом магнитуды. Все 8
    угроз отработаны (W1 частично, W2–W8 закрыты).
  - Отчёт: REPORT.md актуализирован, REPORT_humanized.md пересоздан через /humanizer-ru.
- **Синк с Ульяной (28.05).** Завели Фазу 5 (расширение + разделение труда). Ульяна
  берёт независимый трек («традиционные методы»), запускает расширение по моделям/
  слоям и добавляет 3-го судью (deepseek); общий — наш судейский промпт. Ключевой пункт — человеческая разметка ~300 пар (5.3), чтобы
  решить «энкодер vs LLM-судья» (Ульяна резонно сомневается в формулировке «энкодер
  хуже»). Плюс A+V-сравнение (5.1) для интерпретации корреляций. Общий фрейминг —
  «минимальное представление эмоций» + контроль векторами понятнее промпта.
