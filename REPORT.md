# Emotion steering vectors in LLMs — interim report

Научная практика, 2-й семестр. Перенос метода persona/activation-vectors
(контрастные direction-векторы в пространстве активаций) с черт личности на
**эмоции**. Базовый код — PERSONA (xcfcode), метод — Persona Vectors
(arXiv 2507.21509). Командный датасет — Uliana Kliuchnikova.

## Кратко

Перенесли метод контрастных активационных векторов с черт личности на 7 эмоций
ISEAR и проверили, можно ли управлять конкретной эмоцией. **Стиринг наводит
целевую эмоцию** — при правильной выборке (n=56) это значимо для 6/7 эмоций.
Эмоц-векторы при этом сильно скоррелированы (косинусы 0.45–0.86, переносится на
Gemma). Специфичность мерили достоверным образом — судьёй на n=56 с CI: стиринг
наводит целевую эмоцию значимо по всем 7, **target доминирует у 6/7**, но есть и
значимая **вторичная** ко-активация sadness (+28 [+20,+37]) — целевая сильнее,
близкий негативный кластер тянется следом. **Граница:** на этой задаче простой
промптинг не уступает стирингу (и даже специфичнее, 7/7 argmax), так что
практический выигрыш активационного стиринга здесь не показан. (Оговорка про
измеритель: классификатор-энкодер и LLM-судья дают разную картину специфичности, и
ни один пока не объявлен «истиной»; энкодер структурно слеп к guilt/disgust и шумен
на малых выборках, но на остальных 5 эмоциях может быть менее предвзят — кто ближе к
людям, решаем человеческой разметкой. Здесь основные числа приводим по судье, потому
что он покрывает все 7 эмоций.) Отдельный, устойчивый методологический результат: для
эмоц-направлений проекция на SAE не расщепляет (Jaccard 0.91), а контраст активаций
— расщепляет (0.25) на уровне **отбора фич** (в поведенческую специфичность стиринга
при равной силе это, однако, не переходит — §7б/W2). А там, где стиринг всё же обходит
промптинг — **композиционность**:
можно значимо смешать эмоции (joy+sadness — обе вверх) и вычесть нежелательную
(anger−sadness гасит протечку в sadness, системно у всех 5 протекающих эмоций),
чего инструкцией не дозируешь. Вердикт **устойчив**: независимый второй судья
(qwen-2.5-72b) согласуется сильно (Pearson 0.90, κ 0.82) и даёт ту же картину; а
«минимальность» вектора измерена свипом top-k (~4–8 фич хватает большинству эмоций).

## Задача и подход

Можно ли управлять **конкретной эмоцией** в генерации LLM, добавляя в residual
stream направление, извлечённое из контрастных пар «эмоциональный vs нейтральный
ответ»? И — насколько такое управление **специфично** (целевая эмоция vs прочие)?

Пайплайн: датасет (7 эмоций ISEAR) → независимое **измерение** эмоции (классификатор
+ LLM-судья) → извлечение **векторов** → **стиринг** → анализ **специфичности** →
первый шаг **расщепления**.

## Данные

7 эмоций ISEAR (anger, disgust, fear, guilt, joy, sadness, shame). На каждую —
~50–60 сценариев, разделённых на train и held-out. Для каждой пары инструкций
генерируются ответы Qwen2.5-3B-Instruct: `pos` (эмоциональный) и `neg`
(нейтральный) — контрастные пары.

**Train/test split.** Векторы, judge-фильтрация и SAE-фичи считаются на
**train**-сценариях; стиринг и матрицы специфичности оцениваются на **held-out**
сценариях. То есть отбор направлений/фич и их проверка не пересекаются по данным.

## 1. Измерение эмоции (мерило)

Два независимых измерителя текста:

- **Классификатор-энкодер** `text→вектор`: `SamLowe/roberta-base-go_emotions`
  (GoEmotions, 28 меток) с агрегацией в 7 ISEAR. Метрика — **AUC** (разделимость
  pos vs neg, порого-независимая).
- **Pairwise LLM-судья** (`meta-llama/llama-3.3-70b-instruct` через OpenRouter):
  прямое сравнение pos vs neg ответа, 0–100 (50 = равно). Это согласованный с
  командой редизайн вместо одиночной шкалы.

**Энкодер (AUC по эмоциям):** disgust 0.82, anger 0.78, shame 0.69, fear 0.70,
sadness 0.64, joy 0.62, guilt 0.62; mean AUC 0.697. Все > 0.5 — энкодер разделяет
pos/neg по всем эмоциям. *(`results/encoder_scoring_qwen2.5-3b.md`)*

Энкодер дорабатывали: в первой версии нормировка вектора по 7 ISEAR + широкий
joy-кластер «залипали» (joy: pos 0.948 / neg 0.886, всё стекалось в joy). В v2
убрали нормировку (GoEmotions multi-label) и сузили joy-кластер → mean shift
вырос 0.127 → 0.173, и появилась устойчивая AUC-метрика.

**Судья + кросс-проверка:** mean скоры 66.5–77.4, **832/920 пар** проходят порог 60.
Оба измерителя согласны по 6 из 7 эмоций. **guilt** — судья видит сильный контраст
(74.2), а энкодер слабый (AUC 0.62): это **ограничение энкодера** (GoEmotions
`remorse` плохо ловит вину), не датасета. *(`results/judge_vs_encoder_qwen2.5-3b.md`)*

## 2. Извлечение векторов

Для 832 judge-отфильтрованных пар: residual-stream активации Qwen2.5-3B,
`mean(pos) − mean(neg)` по слоям → 7 эмоц-векторов `[37, 2048]` (как в PERSONA,
формат совместим со стирингом). Прогон на домашнем RTX 2070.

**Находка:** попарные косинусы между эмоц-векторами **0.58–0.86** (все
положительны) — вектора НЕ ортогональны, доминирует общая компонента
«эмоционально vs нейтрально»; joy обособлен по валентности.
*(`results/emotion_vectors_qwen2.5-3b.md`)*

**Почему скоррелированы — valence/arousal-чтение.** Одна общая ось забирает **~80%**
дисперсии 7 векторов (на Gemma — 76%): это «эмоциональность vs нейтрально», отсюда
высокие косинусы (а не отсутствие структуры). В **остатке** после её вычитания
проявляется **валентность** — остаточная PC1 коррелирует с канонической валентностью
(Spearman +0.70 и на Qwen, и на Gemma); arousal в геометрии чисто не выделяется.
Эмпирически то же: по баллам судьи joy **анти-коррелирует** с негативным кластером
(средн. −0.51), а негативные слабо со-активируются. То есть запутанность = общая ось
эмоциональности + валентностный остаток. *(`results/valence_arousal_qwen.md`)*

## 3. Стиринг работает

Добавление anger-вектора в `model.layers[14]` на нейтральных held-out промптах:
anger-скор **0.033 → 0.226 → 0.465** (coeff 0/4/8), монотонно; при coeff 8 — выше,
чем у инструктированных pos-ответов (0.362). Генерация связная и по теме.
*(`results/steering_anger_qwen2.5-3b.md`)*

## 4. Специфичность сырого стиринга

Матрица 7×7 (стиринг к строке → Δ измеряемой эмоции vs baseline, layer 14, coeff 8):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
    anger +0.16 +0.00 -0.00 +0.04 -0.04 +0.23 -0.04
  disgust +0.16 +0.03 +0.13 +0.01 -0.09 +0.17 +0.08
     fear +0.05 +0.01 +0.15 -0.04 +0.00 +0.10 +0.02
    guilt +0.04 +0.00 +0.02 +0.01 +0.04 +0.19 -0.01
      joy -0.00 +0.00 -0.16 -0.03 +0.46 -0.15 +0.07
  sadness +0.08 +0.00 -0.07 -0.04 +0.00 +0.33 -0.02
    shame +0.01 +0.01 +0.09 -0.03 +0.06 +0.18 +0.07
```

- **Специфичные** (диагональ — максимум): **joy** (+0.46), **sadness** (+0.33),
  **fear** (+0.15).
- **Протекающие**: anger (+0.16, но sadness +0.23), disgust/guilt/shame — целевая
  почти не растёт, всё тянет вверх **sadness**.
- **«Аттрактор sadness»**: почти любой стиринг поднимает грусть — на этой матрице.
  Вывод напрашивался: сырой стиринг ≈ общий аффект, а не конкретная эмоция.
  *(`results/steer_specificity_qwen2.5-3b.md`)*

⚠️ **Осторожно: это n=14, энкодер.** На Gemma при n=56 «аттрактор sadness» исчезает
(протечка ~0, см. §8/`bootstrap_ci_n56.md`). Значит вывод про «общий аффект» может
быть артефактом малой выборки — эту матрицу нужно перепроверить на бо́льшем n
(и судьёй). Что устойчиво при n=56 — это сам факт, что стиринг поднимает целевую
эмоцию (диагональ).

## 5. Расщепление (E0): вычитание общей компоненты

Вычли средний по 7 вектор (ренорм к исходной длине), повторили матрицу:

- **Аттрактор sadness убран**: протечка в sadness при стиринге к не-sadness
  эмоциям **+0.12 → −0.03**.
- **joy стал чище и сильнее** (+0.46 → +0.71); fear остался специфичным.
- **Но переусердствует**: **sadness схлопывается** (+0.33 → +0.02) — общий вектор и
  был по сути направлением sadness; часть эмоций сменили протечку (anger→guilt,
  disgust→anger). *(`results/steer_specificity_centered_qwen2.5-3b.md`)*

Mean-subtraction — грубый инструмент: убирает доминирующую ось, но не изолирует
каждую эмоцию. → мотивация для **SAE** (разреженный разбор, «минимальный эмоц-вектор»).

## 6. Расщепление через SAE (E1)

Перенесли векторы на **gemma-2-2b** (есть готовые SAE GemmaScope): эмоц-векторы
Gemma так же скоррелированы (косинусы 0.45–0.83) — **запутанность не специфична
для модели**. Затем спроецировали эмоц-направления на энкодер GemmaScope SAE
(`layer_12/width_16k`) и взяли топ-фичи.

- Наивная проекция: топ-фичи **почти идентичны** у всех эмоций, mean Jaccard **0.91**.
- После центрирования (вычесть общую компоненту, потом проекция): **0.84** — почти
  не помогло.

Вывод: **проекция difference-направления на SAE не изолирует эмоция-специфичные
фичи** — несколько доминирующих фич забирают проекцию у всех. Запутанность
устойчива по всем срезам: косинусы → специфичность → центрирование → SAE-проекция.
*(`results/gemma_sae_layer12.md`)*

## 7. Контрастные активации SAE дают эмоция-специфичные фичи (E1c)

Правильный метод: прогнать реальные pos/neg ответы через Gemma+SAE, ранжировать
фичи по **mean(pos)−mean(neg)** активаций (контраст гасит общий фон). Результат —
эмоции занимают **разные** фичи:

| метод | mean off-diag Jaccard |
|-------|----------------------:|
| проекция направления, наивная | 0.91 |
| проекция, центрированная | 0.84 |
| **контрастные активации** | **0.25** (min 0.08) |

Есть общие фичи (общий аффект), но у каждой эмоции — свои (anger 8517/16003/14599,
guilt 1041/8567, fear 621, joy 6648). **Методологический урок:** узким местом был
не SAE, а способ — проекция направления проваливается, контраст активаций
расщепляет. Это разреженный эмоция-специфичный набор фич. *(`results/sae_contrastive_layer12.md`)*

### 7а. Сколько фич достаточно — свип top-k (W6, «минимальность» измерена)

Собирали steering-вектор каждой эмоции только из top-k контрастных фич (k∈{1,2,4,8,20})
и судили эффект (судья + bootstrap CI, n=28 held-out):

| k фич/эмоцию | средний эффект диагонали | значимых диагоналей |
|---:|---:|:--:|
| 1 | +5 | 1/7 |
| 4 | +19 | 5/7 |
| 8 | +27 | 5/7 |
| 20 | +40 | 7/7 |

- Качество **монотонно растёт** с k: «горстки» ~4–8 фич хватает, чтобы большинство
  эмоций стирилось значимо; все 7 — только на полных 20.
- Нужное число фич **эмоция-зависимо**: guilt стирится **одной** фичей (+42 при k=1),
  а joy не заводится до k=20 (размазан по многим фичам).
- **Меньше фич ≠ чище**: сигнал/протечка не улучшается при урезании (k=4 ≈1:1,
  k=20 ≈1.5:1) — топ-фичи несут и общий аффект, поэтому «минимальный» вектор просто
  слабее, а не специфичнее. Это уточняет вольное прежнее «минимальный».
  *(`results/topk_sweep_gemma.md`)*

### 7б. Почему расщепление не переходит в специфичность (механизм)

Фичи расщепляются на уровне **индексов** (Jaccard 0.25), но steering-вектор — это они,
**декодированные обратно** в residual (Σ Δ·W_dec[f]). Сравнили попарную запутанность
(средний off-diag косинус, layer 12) трёх наборов:

| набор векторов | mean off-diag cos | остаток общей оси |
|---|---:|---:|
| сырые | +0.66 | 0.84 |
| центрированные (raw − общая ось) | **−0.16** | 0.27 |
| SAE-фичи (декод) | +0.61 | 0.48 |

**Декодированный SAE-вектор почти так же запутан, как сырой** (+0.61 vs +0.66) и
сохраняет ~половину общей «эмоциональной» оси (0.48). То есть расщепление на уровне фич
**плохо переживает декод** (строки W_dec сами скоррелированы, общие фичи доминируют в
сумме) — вот почему специфичность стиринга не растёт (W2). Простое центрирование даёт
куда меньший косинус (−0.16), но ценой пере-коррекции (§5 схлопывает sadness). Вывод:
разреженность на уровне фич ≠ ортогональность направлений стиринга.
*(`results/mechanism_ablation_gemma.md`)*

## 8. Стиринг через SAE-фичи

Декодировали отобранные эмоц-фичи обратно в residual (Σ Δ·W_dec[f]) и стирили
ими gemma-2-2b, рядом с сырым вектором (матрица специфичности, layer 12, coeff 8):

| | mean диагональ | target=argmax | **протечка в sadness** |
|---|---:|---:|---:|
| сырой вектор | +0.20 | 3/7 | **+0.10** |
| SAE-фичи | +0.16 | 3/7 | **+0.05** |

Полные матрицы Δ (стиринг к строке → измеряемая эмоция), сырой vs SAE-фичи:

```
сырой       ange  disg  fear  guil   joy  sadn  sham
   anger +0.23 +0.01 -0.01 -0.01 -0.17 +0.25 +0.11
 disgust +0.22 +0.15 -0.03 +0.08 -0.18 +0.19 -0.05
    fear +0.09 +0.01 +0.22 -0.01 -0.13 +0.03 +0.04
   guilt +0.02 +0.00 +0.10 +0.06 -0.18 +0.22 -0.05
     joy -0.03 -0.01 -0.07 -0.02 +0.39 -0.16 +0.01
 sadness +0.11 +0.02 -0.08 -0.03 -0.08 +0.22 -0.06
   shame +0.21 +0.06 +0.03 +0.09 -0.13 +0.06 +0.10

SAE-фичи    ange  disg  fear  guil   joy  sadn  sham
   anger +0.14 +0.04 +0.19 +0.03 -0.16 +0.01 +0.06
 disgust +0.02 +0.06 +0.11 +0.00 -0.05 +0.04 +0.07
    fear +0.02 +0.00 +0.13 -0.02 -0.16 +0.06 -0.00
   guilt +0.12 +0.01 -0.11 +0.06 -0.18 +0.22 -0.04
     joy -0.03 -0.01 -0.06 +0.03 +0.40 -0.04 -0.00
 sadness +0.06 +0.02 +0.09 -0.02 -0.16 +0.19 -0.06
   shame +0.19 +0.03 +0.03 -0.01 -0.16 +0.01 +0.16
```

На малой выборке (n=14, энкодер) казалось, что feature-стиринг вдвое срезает
«аттрактор sadness» (raw +0.10 → SAE +0.05). При n=56 это **не подтвердилось**:
энкодерная протечка ~0 у обоих (raw +0.011 [−0.08,+0.09], SAE +0.020 [−0.07,+0.10]),
разницы raw vs SAE-фичи нет. Но энкодер здесь и сам ненадёжен — достоверный вердикт
по специфичности даёт судья на n=56 (ниже). Что устойчиво по энкодеру при n=56:
диагонали значимы у 6/7 (стиринг наводит целевую эмоцию).
*(`results/steer_sae_feature_gemma.md`, `results/bootstrap_ci_n56.md`)*

**Связность.** Стиринг при coeff 8 не ломает текст: судья по связности даёт всем
условиям ≥ 82/100 (baseline 95), падение умеренное (−3 у sadness/guilt … −13 у
disgust). То есть специфичность мы мерили на связных генерациях, а не на «каше».
*(`results/coherence_gemma_saefeat.md`)*

**Проверка судьёй (guilt/disgust — слепая зона энкодера).** Просудили стиреные
ответы LLM-судьёй: стиринг наводит целевую эмоцию по всем проверенным —
guilt 32→78 (+46), disgust 10→37, anger 14→41, joy 21→55.
«Провал» guilt/disgust в энкодерной матрице был **артефактом измерения**
(GoEmotions их не видит), а не стиринга. *(`results/judge_steered_gemma_saefeat.md`)*

**Достоверная матрица специфичности (судья, n=56, с CI).** Просудили SAE-feature
стиреные ответы по всем 7 эмоциям на 56 промптах/условие (диагональ в `[]`):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
   anger   [+36]  +11   +28   +20   -12   +32   +24
 disgust    +28  [+29]  +39   +22    -7   +34   +29
    fear    +16   +11  [+50]  +19    -5   +24   +16
   guilt     +9    +9   +20  [+49]  -18   +44   +42
     joy     -8    -6   -14   -12  [+33]  -12   -13
 sadness    +24   +17   +38   +32   -10  [+44]  +26
   shame    +28   +16   +48   +54   -18   +48  [+56]
```

**Все 7 диагоналей значимы** (bootstrap 95% CI без 0): shame +57 [46,67],
fear +48 [36,60], guilt +49 [37,60], sadness +44 [33,54], anger +36 [25,47],
joy +34 [20,48], disgust +29 [19,40]. **target=argmax у 6/7** (кроме disgust:
fear +39 > disgust +29). Протечка в sadness **+28 [+20,+37] — значима**: есть
реальная вторичная ко-активация негативного кластера (sadness/guilt/fear/shame
растут вместе), но целевая эмоция почти всегда сильнее. *(`results/judge_specificity_n56.md`)*

**Оговорка про измеритель.** Энкодер и судья дают разную картину специфичности: на
n=14 энкодер давал ложную структуру, на n=56 наоборот *смыл* протечку (~0), тогда как
судья её видит (+28). Это **не** значит автоматически, что энкодер «хуже»: возможен и
обоснованный сдвиг (LLM-судья тоже невалидирован и может иметь свою предвзятость).
Твёрдое ограничение энкодера — он структурно слеп к guilt/disgust (в GoEmotions их
нет). Поэтому основные числа здесь приводим по судье (он покрывает все 7 эмоций), а
вопрос «кто ближе к людям, энкодер или судья» оставляем открытым и решаем
человеческой разметкой (~300 примеров, single-emotion 0–100; см. план 5.3).

**Baseline «промптинг» (W7): стиринг его не превосходит.** Сгенерили с pos-инструкцией
эмоции (без вектора) на тех же промптах и просудили (n=56, CI). Промптинг **сильнее**
по диагонали у 5/7 (joy +57 vs +34, guilt +58 vs +49, anger/disgust выше),
**специфичнее** (протечка sadness +18 [9,27] против +28 [20,37] у стиринга) и даёт
**target=argmax 7/7** (против 6/7). То есть на задаче «навести эмоцию в нейтральном
промпте» активационный стиринг через SAE-фичи **выигрыша над инструкцией не даёт** —
даже чуть уступает. Отличительная ценность стиринга (композиционность, управление без
промпта) здесь не проверялась. *(`results/steer_vs_prompt_n56.md`)*

## 9. Композиционность — где стиринг обходит промптинг

Стирим линейными комбинациями SAE-feature векторов (судья n=56, CI). Это как раз
то, что промптингом не дозируешь:

- **Сплав `joy+sadness`:** одновременно joy **+17 [+3,+31]** И sadness
  **+26 [+14,+38]** — обе значимо вверх (управляемый «горько-сладкий» микс; у
  joy-одного sadness −13, у sadness-одного joy −11).
- **Вычитание `anger − sadness` убирает «аттрактор sadness»:** ко-активация sadness
  +31 → **−6 [−16,+5]** (снижение +37 [+25,+49], значимо), а anger остаётся
  значимым и доминирующим (+37 → +20). Арифметика векторов **хирургически удаляет**
  нежелательную эмоцию.
- **`joy − sadness`** заостряет цель: joy +33 → **+50 [+38,+62]**, sadness глубже
  −14 → **−28 [−36,−20]**.

**Системность вычитания (X − sadness для всех протекающих эмоций, судья n=56 + CI).**
Чтобы проверить, что `anger−sadness` — не единичный трюк, прогнали вычитание sadness
для всех 5 эмоций с заметной протечкой. Снижение sadness значимо у **всех 5** (CI без
0): anger +39 [+27,+51], disgust +29 [+17,+41], fear +33 [+21,+45], guilt +41 [+31,+50],
shame +34 [+25,+44]. Целевая эмоция при этом сохраняется значимо у **4/5** (anger +18,
disgust +11, fear +20, shame +34). **Исключение — guilt:** вычитание гасит и саму вину
(+3 [−9,+15], не значимо) — guilt и sadness в этой модели слиты в один кластер
(согласуется с судейской матрицей guilt~shame~sadness). *(`results/compose_subtract_gemma.md`)*

Это и есть отличительная ценность активационного стиринга: дозируемое **смешивание**
и **вычитание** эмоций (в т.ч. системное гашение найденного аттрактора) — простым
промптингом не достижимо. Цена вычитания — небольшое ослабление целевой эмоции,
регулируется коэффициентом; для очень близких эмоций (guilt↔sadness) вычитание не
разделяет их чисто. *(`results/compose_gemma.md`, `results/compose_subtract_gemma.md`)*

### 9а. Управление вне распределения — слабое при coeff 8

Проверили вторую гипотезу о ценности стиринга — «контроль независимо от промпта»: тот же
SAE-стиринг (coeff 8) на 30 **фактологических** промптах («как заварить чай»). Результат
отрезвляющий: anger/fear/guilt/sadness дают **ровно 0** (ответы остаются фактическими,
почти идентичны baseline), пробиваются только joy **+30 [+18,+42]** и слабее shame +18,
disgust +8 (на эмоц-промптах те же векторы давали +28..+57). То есть стиринг **усиливает
то, что допускает промпт**, а не навязывает эмоцию любому тексту: жёсткая инструкция
перебивает вектор на этом коэффициенте. «Управление без/против промпта» при coeff 8 не
подтвердилось (для перебоя нужен бо́льший coeff — не проверяли, вероятно ценой связности).
Отличительная ценность стиринга остаётся за композиционностью (§9).
*(`results/random_prompts_gemma.md`)*

## Выводы

1. **Стиринг наводит целевую эмоцию — устойчиво.** При n=56 диагональ значима у 6/7
   эмоций, на связном тексте (≥82/100). Это твёрдый положительный результат.
2. **Специфичность (достоверно, судья n=56 + CI):** стиринг наводит целевую эмоцию
   значимо по всем 7, target доминирует у 6/7; при этом значимая **вторичная**
   ко-активация sadness (+28 [+20,+37]) и близкого негативного кластера. То есть
   управление в основном специфично, с честной вторичной протечкой в смежные эмоции.
3. Векторы эмоций сильно скоррелированы (косинусы 0.45–0.86, и на Qwen, и на Gemma).
   Простое центрирование убирает доминирующую ось, но over-корректирует — грубый
   инструмент.
4. **Устойчивый методологический результат:** для эмоц-направлений проекция на SAE не
   расщепляет (Jaccard 0.91), а контраст активаций — расщепляет (0.25). Узкое место —
   метод, не SAE. Это разреженный эмоция-специфичный набор фич. Минимальность измерена
   свипом top-k (§7а): ~4–8 фич хватает большинству эмоций (guilt — 1, joy — все 20),
   но урезание фич не повышает специфичность. Важная граница (W2): расщепление реально
   на уровне *отбора фич* (Jaccard 0.25), но в *поведенческую* специфичность стиринга
   при равной силе не переходит — при уравненном эффекте диагонали SAE-вектор не
   специфичнее сырого (`results/coeffmatch_gemma.md`).
5. **Урок про измеритель:** энкодер и судья дают разную картину специфичности (на n=14
   энкодер давал ложную структуру, на n=56 *смыл* протечку, которую судья видит).
   Прежний claim «SAE-фичи срезают протечку» оказался артефактом энкодера на малом n.
   Кто из измерителей точнее — пока **открытый вопрос**: энкодер слеп к guilt/disgust и
   шумен, но на остальных 5 может быть менее предвзят, чем (тоже невалидированный)
   судья. Основные числа приводим по судье (покрывает все 7), а арбитраж «энкодер vs
   судья» делаем человеческой разметкой (план 5.3) с CI.
6. **Граница (промптинг-baseline):** на задаче «навести эмоцию в нейтральном
   промпте» простой промптинг не уступает стирингу (и даже специфичнее, 7/7 argmax)
   — практического выигрыша активационного стиринга здесь нет.
7. **Где стиринг всё же ценен — композиционность (значимо, судья+CI):** дозируемый
   сплав (joy+sadness: обе эмоции вверх) и вычитание (anger−sadness гасит «аттрактор
   sadness», anger остаётся доминирующим; joy−sadness заостряет joy). Вычитание
   sadness **системно** убирает аттрактор у всех 5 протекающих эмоций (снижение
   значимо везде, целевая эмоция сохраняется у 4/5; граница — слитые guilt↔sadness).
   Векторная арифметика даёт контроль, недостижимый простым промптингом — это и есть
   отличительная ценность метода.
8. **Вердикт устойчив к выбору судьи (W4):** независимый судья другого семейства
   (qwen-2.5-72b) на тех же ответах согласуется с основным сильно (Pearson 0.90,
   Cohen's κ 0.82, argmax 6/7) и даёт ту же картину (все 7 диагоналей значимы,
   значимая протечка sadness). Единственное расхождение — guilt↔sadness.

## Угрозы валидности

Честно перечисляем, где результаты пока слабы (план усиления — `IMPROVEMENT_PLAN.md`):

- **Выборка/погрешности (W1) — частично закрыто.** Главная (судейская) матрица
  специфичности теперь на n=56 с bootstrap 95% CI (все 7 диагоналей значимы,
  протечка sadness значима). Coeff-matched raw vs SAE тоже на CI (W2). Остаётся малым
  только ранняя матрица §4 на Qwen (n=14) — её ещё можно добить до n=56.
- **Магнитуда raw vs SAE-фичи (W2) — закрыто, результат отрезвляющий.** Свели coeff
  {4,8,16} для обоих типов и построили фронтир «протечка vs эффект диагонали» (судья +
  CI). При одном coeff SAE стирит слабее (диагональ 16<20, 39<45, 60<70). При
  сопоставимом эффекте диагонали SAE **не специфичнее** raw, а на высокой силе даже
  протекает больше (при диаг ~60 протечка raw ≈27 vs sae +36). Прежняя видимость
  «SAE меньше протекает» оказалась артефактом магнитуды: слабый стиринг тривиально
  меньше протекает. Расщепление SAE реально на уровне отбора фич (Jaccard 0.25), но в
  поведенческую специфичность при равной силе не переходит. *(`results/coeffmatch_gemma.md`)*
- **Смешение модель×измеритель (W3) — закрыто по основной линии.** Вся достоверная
  линия результатов идёт **на одной модели одним мерителем**: gemma-2-2b + LLM-судья —
  стиринг (§8), специфичность n=56 (§8), композиция (§9), системное вычитание (§9),
  свип top-k (§7а), валидация вторым судьёй (W4). Ранний Qwen + энкодер (§4–5) — это
  разведочная часть, и именно её несогласованности (энкодер «смыл» протечку) мы и
  показываем как урок про измеритель. То есть сквозной single-model/single-measurer
  прогон есть; смешение осталось только в ранних разделах, помеченных как
  exploratory.
- **Судья один и невалидирован (W4) — закрыто по части второго судьи.** Пересудили
  те же n=56 ответов независимым судьёй другого семейства (qwen-2.5-72b): согласие
  сильное — Pearson r=0.90, Spearman ρ=0.90 (2940 пар), Cohen's κ=0.82, argmax по
  диагонали совпал 6/7. Оба судьи независимо дают все 7 диагоналей значимыми и
  значимую вторичную протечку sadness (llama +28 [+20,+37], qwen +24 [+16,+31]) —
  вердикт не артефакт одной модели. Единственное расхождение — guilt (qwen видит там
  sadness), та же слитость guilt↔sadness. Остаётся опционально добавить человеческие
  метки (`results/judge_agreement_n56.md`).
- **Связность (W5) — измерена** (§8): при coeff 8 текст не ломается (≥82/100), но
  оценка тем же семейством судьи и на n=14.
- **«Минимальность» (W6) — измерена.** Свип top-k (§7а): качество стиринга растёт с
  k, ~4–8 фич хватает большинству эмоций (значимо), guilt — 1 фича, joy — все 20;
  урезание фич не повышает специфичность (сигнал/протечка не растёт). Слово
  «минимальный» теперь подкреплено числами, а не заявлено.
- **Baseline «промптинг» (W7) — закрыто, и результат отрезвляющий:** промптинг не
  уступает стирингу (даже специфичнее). Выигрыш стиринга над инструкцией на этой
  задаче не показан (§8).
- **«Аттрактор sadness» (W8) — закрыто.** Судья на n=56 с CI: ко-активация sadness
  значима (+28 [+20,+37]), но вторична относительно целевой эмоции. Реальный эффект,
  не артефакт — **подтверждён вторым судьёй** (qwen: +24 [+16,+31], см. W4). Нюанс:
  в нейтральных (нестиреных) ответах sadness и так самая высокая эмоция (~32/100,
  оба судьи) — частично это базовый меланхоличный приор промптов; но протечка считается
  как Δ **над** baseline, т.е. стиринг поднимает sadness *поверх* приора
  (`results/baseline_emotion_prior.md`).
- Прочее: эмоции инструкция-индуцированы (не естественны); стиринг `positions=all`,
  один слой, без подбора на эмоцию; guilt — всего 42 пары.

## Дальше

Сделано в ходе усиления (см. `IMPROVEMENT_PLAN.md`): bootstrap CI, судья n=56,
coherence, промптинг-baseline (W7), композиционность + системное вычитание (§9),
свип top-k / «минимальность» (§7а, W6), второй судья (W4), базовый приор (W8),
сквозная линия на одной модели/мерителе (W3), coeff-matched raw vs SAE (W2). Осталось:

- Больше моделей/слоёв (перенос и обобщаемость); SAE на нескольких слоях.
- Естественные (не инструкция-индуцированные) эмоции — внешняя валидация векторов.
- Опционально: 20–30 человеческих меток к судье; механизм «почему SAE помогает»
  (top-k фичи vs «raw минус общие фичи»).
- Согласовать рамку новизны и деление вклада с Уляной.

## Воспроизводимость

Репозиторий: github.com/dmagog/persona-emotions, ветка `emotion-encoder`.
Код в `emotion/`: измерение — `space`, `encoder`, `classifier_encoder`,
`metrics`, `score_csv`; судья — `pairwise_judge`, `run_pairwise_judge`,
`judge_steered`, `judge_specificity`, `judge_agreement` (согласие судей);
статистика — `bootstrap_ci`, `coherence_check`; векторы и стиринг —
`extract_vectors`, `steer_eval`, `steer_specificity`, `steer_compose`
(сплав/вычитание); SAE — `gemma_sae`, `sae_contrastive`, `build_sae_vectors`;
свипы — `steer_topk_sweep`+`judge_topk_sweep`+`topk_summary` (минимальность),
`steer_coeffmatch_sweep`+`judge_coeffmatch_sweep`+`coeffmatch_summary`
(raw vs SAE при равной магнитуде). Воспроизводящие ноутбуки — `notebooks/`.
Артефакты с числами и матрицами — `results/*.md` + CSV/JSON. Компьют: домашний
RTX 2070 (извлечение/стиринг/SAE/свипы на Qwen2.5-3B и gemma-2-2b), Kaggle
(энкодер на CPU), OpenRouter (LLM-судьи llama-3.3-70b и qwen-2.5-72b).

## Вклад

- **Uliana Kliuchnikova**: датасет (7 эмоций), генерация ответов, базовый пайплайн.
- **Данная работа**: блок измерения (энкодер + AUC, pairwise-судья + кросс-проверка),
  извлечение эмоц-векторов и стиринг, матрицы специфичности, расщепление
  (центрирование E0; SAE-проекция E1; контрастные активации SAE E1c — минимальный
  эмоц-вектор), стиринг через SAE-фичи и судейская оценка специфичности; усиление
  методологии: bootstrap CI, судья на n=56, проверка связности, baseline-промптинг,
  композиционность и системное вычитание, свип top-k («минимальность»), валидация
  вторым судьёй (согласие), базовый эмоц-приор, coeff-matched сравнение raw vs SAE.
