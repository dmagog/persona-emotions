# Emotion steering vectors in LLMs — interim report

Научная практика, 2-й семестр. Перенос метода persona/activation-vectors
(контрастные direction-векторы в пространстве активаций) с черт личности на
**эмоции**. Базовый код — PERSONA (xcfcode), метод — Persona Vectors
(arXiv 2507.21509). Командный датасет — Uliana Kliuchnikova.

## Кратко

Перенесли метод контрастных активационных векторов с черт личности на 7 эмоций
ISEAR и проверили, можно ли управлять конкретной эмоцией. Стиринг **работает**
(целевая эмоция растёт монотонно, генерация связная), но сырой вектор управляет
**общим аффектом**: почти любой стиринг тянет в sadness, а сами эмоц-векторы
сильно скоррелированы (косинусы 0.45–0.86) — и это переносится на другую модель
(Gemma). Центрирование и проекция на SAE запутанность не снимают. Снимает её
**контрастный разбор активаций SAE**: эмоции занимают разные разреженные фичи
(Jaccard 0.25 против 0.84–0.91 у проекции). Стиринг через эти фичи вдвое уменьшает
протечку в sadness, а по оценке LLM-судьи наводит целевую эмоцию по всем 7
(специфично для 4/7; промахи идут по семантически близким эмоциям).

Самый крепкий и воспроизводимый вклад — **методологический**: для эмоц-направлений
проекция на SAE не расщепляет, а контраст активаций — расщепляет (Jaccard
0.91 → 0.25). Заявления про специфичность стиринга пока на малых выборках без
доверительных интервалов и требуют статистики (см. «Угрозы валидности»).

## Задача и подход

Можно ли управлять **конкретной эмоцией** в генерации LLM, добавляя в residual
stream направление, извлечённое из контрастных пар «эмоциональный vs нейтральный
ответ»? И — насколько такое управление **специфично** (целевая эмоция vs прочие)?

Пайплайн: датасет (7 эмоций ISEAR) → независимое **измерение** эмоции (классификатор
+ LLM-судья) → извлечение **векторов** → **стиринг** → анализ **специфичности** →
первый шаг **расщепления**.

## Данные

7 эмоций ISEAR (anger, disgust, fear, guilt, joy, sadness, shame). На каждую —
~50–60 сценариев, разделённых на train и held-out. Для каждой пары инструкций
генерируются ответы Qwen2.5-3B-Instruct: `pos` (эмоциональный) и `neg`
(нейтральный) — контрастные пары.

**Train/test split.** Векторы, judge-фильтрация и SAE-фичи считаются на
**train**-сценариях; стиринг и матрицы специфичности оцениваются на **held-out**
сценариях. То есть отбор направлений/фич и их проверка не пересекаются по данным.

## 1. Измерение эмоции (мерило)

Два независимых измерителя текста:

- **Классификатор-энкодер** `text→вектор`: `SamLowe/roberta-base-go_emotions`
  (GoEmotions, 28 меток) с агрегацией в 7 ISEAR. Метрика — **AUC** (разделимость
  pos vs neg, порого-независимая).
- **Pairwise LLM-судья** (`meta-llama/llama-3.3-70b-instruct` через OpenRouter):
  прямое сравнение pos vs neg ответа, 0–100 (50 = равно). Это согласованный с
  командой редизайн вместо одиночной шкалы.

**Энкодер (AUC по эмоциям):** disgust 0.82, anger 0.78, shame 0.69, fear 0.70,
sadness 0.64, joy 0.62, guilt 0.62; mean AUC 0.697. Все > 0.5 — энкодер разделяет
pos/neg по всем эмоциям. *(`results/encoder_scoring_qwen2.5-3b.md`)*

Энкодер дорабатывали: в первой версии нормировка вектора по 7 ISEAR + широкий
joy-кластер «залипали» (joy: pos 0.948 / neg 0.886, всё стекалось в joy). В v2
убрали нормировку (GoEmotions multi-label) и сузили joy-кластер → mean shift
вырос 0.127 → 0.173, и появилась устойчивая AUC-метрика.

**Судья + кросс-проверка:** mean скоры 66.5–77.4, **832/920 пар** проходят порог 60.
Оба измерителя согласны по 6 из 7 эмоций. **guilt** — судья видит сильный контраст
(74.2), а энкодер слабый (AUC 0.62): это **ограничение энкодера** (GoEmotions
`remorse` плохо ловит вину), не датасета. *(`results/judge_vs_encoder_qwen2.5-3b.md`)*

## 2. Извлечение векторов

Для 832 judge-отфильтрованных пар: residual-stream активации Qwen2.5-3B,
`mean(pos) − mean(neg)` по слоям → 7 эмоц-векторов `[37, 2048]` (как в PERSONA,
формат совместим со стирингом). Прогон на домашнем RTX 2070.

**Находка:** попарные косинусы между эмоц-векторами **0.58–0.86** (все
положительны) — вектора НЕ ортогональны, доминирует общая компонента
«эмоционально vs нейтрально»; joy обособлен по валентности.
*(`results/emotion_vectors_qwen2.5-3b.md`)*

## 3. Стиринг работает

Добавление anger-вектора в `model.layers[14]` на нейтральных held-out промптах:
anger-скор **0.033 → 0.226 → 0.465** (coeff 0/4/8), монотонно; при coeff 8 — выше,
чем у инструктированных pos-ответов (0.362). Генерация связная и по теме.
*(`results/steering_anger_qwen2.5-3b.md`)*

## 4. Специфичность сырого стиринга

Матрица 7×7 (стиринг к строке → Δ измеряемой эмоции vs baseline, layer 14, coeff 8):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
    anger +0.16 +0.00 -0.00 +0.04 -0.04 +0.23 -0.04
  disgust +0.16 +0.03 +0.13 +0.01 -0.09 +0.17 +0.08
     fear +0.05 +0.01 +0.15 -0.04 +0.00 +0.10 +0.02
    guilt +0.04 +0.00 +0.02 +0.01 +0.04 +0.19 -0.01
      joy -0.00 +0.00 -0.16 -0.03 +0.46 -0.15 +0.07
  sadness +0.08 +0.00 -0.07 -0.04 +0.00 +0.33 -0.02
    shame +0.01 +0.01 +0.09 -0.03 +0.06 +0.18 +0.07
```

- **Специфичные** (диагональ — максимум): **joy** (+0.46), **sadness** (+0.33),
  **fear** (+0.15).
- **Протекающие**: anger (+0.16, но sadness +0.23), disgust/guilt/shame — целевая
  почти не растёт, всё тянет вверх **sadness**.
- **«Аттрактор sadness»**: почти любой стиринг поднимает грусть — это проявление
  общей аффект-компоненты (см. косинусы). Вывод: **сырой стиринг ≈ общий аффект**,
  а не конкретная эмоция. *(`results/steer_specificity_qwen2.5-3b.md`)*

## 5. Расщепление (E0): вычитание общей компоненты

Вычли средний по 7 вектор (ренорм к исходной длине), повторили матрицу:

- **Аттрактор sadness убран**: протечка в sadness при стиринге к не-sadness
  эмоциям **+0.12 → −0.03**.
- **joy стал чище и сильнее** (+0.46 → +0.71); fear остался специфичным.
- **Но переусердствует**: **sadness схлопывается** (+0.33 → +0.02) — общий вектор и
  был по сути направлением sadness; часть эмоций сменили протечку (anger→guilt,
  disgust→anger). *(`results/steer_specificity_centered_qwen2.5-3b.md`)*

Mean-subtraction — грубый инструмент: убирает доминирующую ось, но не изолирует
каждую эмоцию. → мотивация для **SAE** (разреженный разбор, «минимальный эмоц-вектор»).

## 6. Расщепление через SAE (E1)

Перенесли векторы на **gemma-2-2b** (есть готовые SAE GemmaScope): эмоц-векторы
Gemma так же скоррелированы (косинусы 0.45–0.83) — **запутанность не специфична
для модели**. Затем спроецировали эмоц-направления на энкодер GemmaScope SAE
(`layer_12/width_16k`) и взяли топ-фичи.

- Наивная проекция: топ-фичи **почти идентичны** у всех эмоций, mean Jaccard **0.91**.
- После центрирования (вычесть общую компоненту, потом проекция): **0.84** — почти
  не помогло.

Вывод: **проекция difference-направления на SAE не изолирует эмоция-специфичные
фичи** — несколько доминирующих фич забирают проекцию у всех. Запутанность
устойчива по всем срезам: косинусы → специфичность → центрирование → SAE-проекция.
*(`results/gemma_sae_layer12.md`)*

## 7. Контрастные активации SAE дают эмоция-специфичные фичи (E1c)

Правильный метод: прогнать реальные pos/neg ответы через Gemma+SAE, ранжировать
фичи по **mean(pos)−mean(neg)** активаций (контраст гасит общий фон). Результат —
эмоции занимают **разные** фичи:

| метод | mean off-diag Jaccard |
|-------|----------------------:|
| проекция направления, наивная | 0.91 |
| проекция, центрированная | 0.84 |
| **контрастные активации** | **0.25** (min 0.08) |

Есть общие фичи (общий аффект), но у каждой эмоции — свои (anger 8517/16003/14599,
guilt 1041/8567, fear 621, joy 6648). **Методологический урок:** узким местом был
не SAE, а способ — проекция направления проваливается, контраст активаций
расщепляет. Это разреженный эмоция-специфичный набор фич; называть его
«минимальным» преждевременно — минимальность (сколько фич достаточно) ещё не
измерена (свип top-k в плане). *(`results/sae_contrastive_layer12.md`)*

## 8. Стиринг через SAE-фичи

Декодировали отобранные эмоц-фичи обратно в residual (Σ Δ·W_dec[f]) и стирили
ими gemma-2-2b, рядом с сырым вектором (матрица специфичности, layer 12, coeff 8):

| | mean диагональ | target=argmax | **протечка в sadness** |
|---|---:|---:|---:|
| сырой вектор | +0.20 | 3/7 | **+0.10** |
| SAE-фичи | +0.16 | 3/7 | **+0.05** |

Полные матрицы Δ (стиринг к строке → измеряемая эмоция), сырой vs SAE-фичи:

```
сырой       ange  disg  fear  guil   joy  sadn  sham
   anger +0.23 +0.01 -0.01 -0.01 -0.17 +0.25 +0.11
 disgust +0.22 +0.15 -0.03 +0.08 -0.18 +0.19 -0.05
    fear +0.09 +0.01 +0.22 -0.01 -0.13 +0.03 +0.04
   guilt +0.02 +0.00 +0.10 +0.06 -0.18 +0.22 -0.05
     joy -0.03 -0.01 -0.07 -0.02 +0.39 -0.16 +0.01
 sadness +0.11 +0.02 -0.08 -0.03 -0.08 +0.22 -0.06
   shame +0.21 +0.06 +0.03 +0.09 -0.13 +0.06 +0.10

SAE-фичи    ange  disg  fear  guil   joy  sadn  sham
   anger +0.14 +0.04 +0.19 +0.03 -0.16 +0.01 +0.06
 disgust +0.02 +0.06 +0.11 +0.00 -0.05 +0.04 +0.07
    fear +0.02 +0.00 +0.13 -0.02 -0.16 +0.06 -0.00
   guilt +0.12 +0.01 -0.11 +0.06 -0.18 +0.22 -0.04
     joy -0.03 -0.01 -0.06 +0.03 +0.40 -0.04 -0.00
 sadness +0.06 +0.02 +0.09 -0.02 -0.16 +0.19 -0.06
   shame +0.19 +0.03 +0.03 -0.01 -0.16 +0.01 +0.16
```

**Feature-стиринг вдвое срезает «аттрактор sadness»** (anger→sadness +0.25→+0.01,
disgust→sadness +0.19→+0.04). Расщепление переносится в управление: разреженный
вектор тянет в общий негатив меньше сырого. Цена — чуть слабее целевой эффект, и
часть протечки перераспределяется (anger→fear). *(`results/steer_sae_feature_gemma.md`)*

**Проверка судьёй (guilt/disgust — слепая зона энкодера).** Просудили стиреные
ответы LLM-судьёй: стиринг наводит целевую эмоцию по всем проверенным —
guilt 32→78 (+46), disgust 10→37, anger 14→41, joy 21→55.
«Провал» guilt/disgust в энкодерной матрице был **артефактом измерения**
(GoEmotions их не видит), а не стиринга. *(`results/judge_steered_gemma_saefeat.md`)*

**Судейская матрица специфичности (надёжная версия).** Просудили все стиреные
ответы по 7 эмоциям независимо от энкодера (диагональ в `[]`):

```
steer\meas  ange  disg  fear  guil   joy  sadn  sham
   anger    +26   +18   +29   +21   -19   +30   +13
 disgust    +23   +25   +30    +6    -5   +33   +18
    fear     +4    +6  [+41]  +16    -3   +24   +13
   guilt     +6    +8   +15  [+46]  -19   +47  [+48]
     joy    -12   -11   -15   -16  [+33]   -8   -10
 sadness    +16   +15   +27   +20   -12  [+45]  +26
   shame    +29   +16   +39   +41   -21   +46  [+56]
```

Диагонали сильные (guilt +46, shame +56, sadness +45, fear +41),
**target=argmax у 4/7** (fear/joy/sadness/shame, против 3/7 у энкодера). Промахи **семантически осмысленны**: anger/disgust тянут
sadness, guilt почти неотличим от shame/sadness — модель группирует
guilt~shame~sadness; joy чисто подавляет негатив. То есть остаточная протечка идёт
по геометрии эмоций, а не шум. *(`results/judge_specificity_gemma_saefeat.md`)*

## Выводы

1. Контрастные эмоц-векторы переносятся с черт на эмоции: стиринг **работает** и
   даёт связную генерацию.
2. Но «сырой» стиринг управляет **общим аффектом**, а не конкретной эмоцией —
   доказано матрицей специфичности и косинусами.
3. Простое центрирование убирает доминирующий аттрактор (joy/fear чище), но
   over-корректирует → нужен умный разбор (SAE).
4. Запутанность **устойчива** (переносится на Gemma, переживает SAE-проекцию), но
   **снимается контрастным разбором активаций SAE**: эмоции занимают разные
   разреженные фичи (Jaccard 0.25 vs 0.84–0.91 у проекции). Узкое место — метод, не
   SAE. Это рабочий «минимальный эмоц-вектор».
5. Стиринг через SAE-фичи в наших прогонах уменьшает протечку в sadness vs сырой
   вектор (+0.10 → +0.05). Это предварительно: малая выборка (n=14, без CI) и не
   уравнена магнитуда стиринга — направление требует подтверждения (см. «Угрозы
   валидности»).

## Угрозы валидности

Честно перечисляем, где результаты пока слабы (план усиления — `IMPROVEMENT_PLAN.md`):

- **Малая выборка, нет погрешностей (W1).** Матрицы специфичности — 14 промптов на
  условие, точечные оценки без доверительных интервалов. Δ порядка ±0.05 могут быть
  шумом. Это главное ограничение.
- **Сравнение raw vs SAE-фичи не контролирует магнитуду (W2).** У SAE-вектора
  диагональ слабее (+0.16 vs +0.20), поэтому «меньше протечки» может частично
  означать «слабее стиринг». Нужно уравнять целевой эффект, потом сравнивать.
- **Смешение модель×измеритель (W3).** §4–5 — Qwen + энкодер; §8 — Gemma + (энкодер
  и судья). Сквозного прогона одной модели одним измерителем нет.
- **Судья один и невалидирован (W4).** `llama-3.3-70b` и фильтрует пары, и судит
  специфичность; сверки с людьми и второго судьи нет.
- **Связность не измерена (W5).** «Генерация связная» — по одному примеру; coherence
  стиреных выходов не считали.
- **«Минимальность» не показана (W6).** Свипа top-k фич нет; называть вектор
  «минимальным» пока преждевременно.
- **Нет baseline «промптинг» (W7).** Не показано, что стиринг даёт сверх прямой
  инструкции.
- **«Аттрактор sadness» недо-проверен (W8).** Может объясняться высоким базовым
  уровнем sadness у измерителей, а не только общей аффект-компонентой.
- Прочее: эмоции инструкция-индуцированы (не естественны); стиринг `positions=all`,
  один слой, без подбора на эмоцию; guilt — всего 42 пары.

## Дальше

- Усилить feature-стиринг: подбор слоя/коэффициента на эмоцию, top-k фич; добиться
  чтобы target=argmax для большего числа эмоций.
- Мерить guilt/disgust **судьёй** (энкодер их не видит) — честная оценка протечки.
- Больше моделей/слоёв; согласовать рамку новизны и деление с Уляной.

## Воспроизводимость

Репозиторий: github.com/dmagog/persona-emotions, ветка `emotion-encoder`.
Код в `emotion/`: измерение — `space`, `encoder`, `classifier_encoder`,
`metrics`, `score_csv`; судья — `pairwise_judge`, `run_pairwise_judge`,
`judge_steered`, `judge_specificity`; векторы и стиринг — `extract_vectors`,
`steer_eval`, `steer_specificity`; SAE — `gemma_sae`, `sae_contrastive`,
`build_sae_vectors`. Воспроизводящие ноутбуки — `notebooks/`. Артефакты с числами
и матрицами — `results/*.md` + CSV/JSON. Компьют: домашний RTX 2070
(извлечение/стиринг/SAE на Qwen2.5-3B и gemma-2-2b), Kaggle (энкодер на CPU),
OpenRouter (LLM-судья).

## Вклад

- **Uliana Kliuchnikova**: датасет (7 эмоций), генерация ответов, базовый пайплайн.
- **Данная работа**: блок измерения (энкодер + AUC, pairwise-судья + кросс-проверка),
  извлечение эмоц-векторов и стиринг, матрицы специфичности, расщепление
  (центрирование E0; SAE-проекция E1; контрастные активации SAE E1c — минимальный
  эмоц-вектор), стиринг через SAE-фичи и судейская оценка специфичности.
